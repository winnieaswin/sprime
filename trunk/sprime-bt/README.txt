Require Libs (in Ubuntu):
- libbluetooth-dev

Required JARs (in bluecove folder):
- bluecove-2.1.0.jar
- bluecove-gpl-2.1.0.jar

In Eclipse:
- Import existing projects into workspace > choose sprime-bt folder
-- everything should work after that
-- if not, create a new java project and import folder sprime-bt
